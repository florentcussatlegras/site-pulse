<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Redirection...</title>
    <script>
        // redirection vers /app après chargement
        window.location.href = "{{ target }}";
    </script>
    <style>
        /* Spinner simple avec animation */
        .spinner {
            border: 8px solid #f3f3f3; /* gris clair */
            border-top: 8px solid #6366f1; /* indigo-500 */
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0%   { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        html, body {
            height: 100%;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f9fafb; /* bg-neutral-50 */
            font-family: sans-serif;
        }
    </style>
</head>
<body>
    <div class="spinner"></div>
</body>
</html>
